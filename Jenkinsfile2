 pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                    rsync -avz --delete ./app/ user@yourserver:/var/www/html/
                '''
            }
        }
    }
}

